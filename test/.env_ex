#!/bin/bash

cat >~/.terraformrc <<EOF
provider_installation {
    filesystem_mirror {
        path    = "$HOME/.terraform.d/plugins"
    }
    network_mirror {
        url = "https://art.gos-tech.xyz/artifactory/api/terraform/terraform-local-pid/providers/"
    }
}
EOF

set -a
#### PD15
#SI_URL=https://portal.gos.sbercloud.dev/api/v1
#SI_TOKEN=

#SI_TIMEOUT=600 #set timeout 10 min
#VM_PASSWORD_OUTPUT=True
#AWS_ACCESS_KEY_ID=
#AWS_SECRET_ACCESS_KEY=
set +a
